# MTO360 FastAPI Application

FastAPI backend with Domain-Driven Design architecture.

## Quick Start

1. Install dependencies: `pip install -e .`
2. Configure `.env` file with database settings
3. Run: `uvicorn app.main:app --reload`

See `README_FASTAPI.md` for detailed documentation.

